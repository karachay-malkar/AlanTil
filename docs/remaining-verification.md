# Mobile / Web 13.15.12 — проверяемый остаток

Эталон: неизменённый Web 13.15.12 (3249e0d). Рабочая ветка: agent/16.6.3-full-screen-parity. Main не изменяется.

| Направление | Что подтверждено | Что пока не подтверждено / препятствие |
|---|---|---|
| Общая логика | Shared core; 20/40/80, направления, восстановление, результаты проверяются в автоматических сценариях | Все ошибки/повторы и аварийное завершение установленного приложения |
| Первоначальная настройка | Последовательное раскрытие одного экрана; убран фиксированный предел высоты 120 | Матрица RU/EN/TR × S/M/L на физических устройствах |
| Подсказки/выход | Веб-прогон проверяет шаги, центр подсветки, очистку, подтверждение/отмену выхода | Native safe-area, системный Back, быстрые касания, все шаги общего обучения |
| Фон/кнопки/текст | Перенесены повторяемые слои фона; исправлены оставшиеся высокие тени контейнеров, семантические размеры песен/диалогов | Полное попарное сравнение Android/Web; Android стеклянные поверхности сейчас используют упрощённую заливку для производительности |
| Путь | Тяжёлое содержимое станций ограничено окном; фон мемоизирован | Измеренный FPS/frame time отсутствует: нет подключённого Android/ADB. Лёгкие строки и линия пути ещё не виртуализированы целиком |
| Синхронизация | Очередь сохраняет новые/обновлённые записи; перенос гостя, загрузка облака и локальные записи привязаны к исходному аккаунту | Одновременные локальные правки и cloud merge не являются общей атомарной транзакцией; отложенные события SDK Auth и полная офлайн/онлайн матрица |
| Авторизация | PKCE/token callback и intent-filter предусмотрены; сетевой сбой refresh не очищает сессию в тестах | Additional Redirect URLs не проверен: доступные Supabase-инструменты не предоставляют Auth Configuration. Реальный Google → приложение, cold start/logout/restart не выполнены без устройства/входа пользователя |
| Песни | Кэш/refresh/offline/error/retry проверены поведенческими тестами; исправлено info | Аудио, seek, звонки/фон/возврат на Android; нет runtime-подтверждения |
| Профиль | Защита от запоздалого ответа предыдущего аккаунта; гостевой экран в веб-матрице | Заполненный аккаунт, смена ника/аватара и реальные облачные значения требуют авторизованной сессии |
| Доступность | Часть ролей/подписей и скрытие закрытых секций реализованы | TalkBack, масштаб системного шрифта и фокус на устройстве |

Последний проверенный веб-артефакт до этого пакета: 69650d7, 49 PASS, без console/page/network errors, 390×844. Новый пакет добавляет проверку доступности кнопки подсказки при 320×480. Успешный веб-прогон не заменяет Android-проверку и не означает полное визуальное соответствие.

Для окончательного закрытия нужны установленная актуальная APK на Android (прокрутка, подсказки, системный Back, вход/перезапуск) и проверка строки alantil://auth/callback в Supabase Authentication → URL Configuration. Предыдущий отказ доступа к Cloud Browser не обходился альтернативным браузером; проверки опубликованного Expo Web выполняются существующим GitHub CI.
