import React,{useEffect,useMemo,useRef}from'react';
import{PanResponder,StyleSheet,View}from'react-native';
import{GLView}from'expo-gl';
import*as THREE from'three';
import{ASHYK_COLORS,BOARD_SCALE,HALF,MAX_PULL,SHOT_CANCEL_RADIUS}from'../../packages/ashyk-game/constants.js';
import{ASHYK_SCENE_MATERIALS,createAshykVisual}from'../../packages/ashyk-game/model/index.js';

function canvasShim(gl){return{width:gl.drawingBufferWidth,height:gl.drawingBufferHeight,style:{},clientWidth:gl.drawingBufferWidth,clientHeight:gl.drawingBufferHeight,addEventListener(){},removeEventListener(){},setAttribute(){},getContext(){return gl;}};}
function pieceFromObject(object,visuals){let current=object;while(current){const id=current.userData?.ashykId;if(typeof id==='number')return{id,visual:visuals.get(id)};current=current.parent;}return null;}

export function AshykNativeScene({engine,interactionUnlocked=true,cameraMode='orbit',shotMode='flat'}){const runtime=useRef(null),layout=useRef({width:1,height:1}),props=useRef({interactionUnlocked,cameraMode,shotMode});props.current={interactionUnlocked,cameraMode,shotMode};
 const updateCamera=(rt)=>{if(!rt)return;if(props.current.cameraMode==='top'){rt.camera.position.set(.01,40,.01);rt.camera.lookAt(0,0,0);return;}const radius=39,yaw=rt.yaw,pitch=Math.max(.38,Math.min(1.08,rt.pitch)),horizontal=Math.cos(pitch)*radius;rt.camera.position.set(Math.sin(yaw)*horizontal,Math.sin(pitch)*radius,Math.cos(yaw)*horizontal);rt.camera.lookAt(0,.3,0);};
 const pointerToRay=(rt,x,y)=>{const {width,height}=layout.current;rt.pointer.x=x/Math.max(1,width)*2-1;rt.pointer.y=-(y/Math.max(1,height))*2+1;rt.raycaster.setFromCamera(rt.pointer,rt.camera);return rt.raycaster;};
 const pan=useMemo(()=>PanResponder.create({onStartShouldSetPanResponder:()=>true,onMoveShouldSetPanResponder:()=>true,onPanResponderGrant:(event)=>{const rt=runtime.current;if(!rt||!props.current.interactionUnlocked||!engine.isReady()||engine.isShotActive())return;const x=event.nativeEvent.locationX,y=event.nativeEvent.locationY,ray=pointerToRay(rt,x,y),hit=ray.intersectObjects([...rt.visuals.values()].filter(item=>item.visible),true)[0],piece=pieceFromObject(hit?.object,rt.visuals);rt.touch={startX:x,startY:y,lastX:x,lastY:y,pieceId:piece?.id??null,aiming:piece?.id===engine.getSelectedId(),command:null};if(rt.touch.aiming)rt.orbiting=false;},onPanResponderMove:(event,gesture)=>{const rt=runtime.current,touch=rt?.touch;if(!rt||!touch)return;const x=event.nativeEvent.locationX,y=event.nativeEvent.locationY,dx=x-touch.startX,dy=y-touch.startY;if(touch.aiming){if(Math.hypot(dx,dy)<5)return;const piece=engine.getPiece(touch.pieceId);if(!piece)return;const ray=pointerToRay(rt,x,y);if(!ray.ray.intersectPlane(rt.boardPlane,rt.hitPoint))return;const ox=rt.hitPoint.x-piece.body.position.x,oz=rt.hitPoint.z-piece.body.position.z,pull=Math.min(MAX_PULL,Math.hypot(ox,oz));if(pull<=SHOT_CANCEL_RADIUS){touch.command=null;rt.dots.forEach(dot=>dot.visible=false);return;}const inv=1/Math.max(.0001,Math.hypot(ox,oz)),directionX=-ox*inv,directionZ=-oz*inv;touch.command={mode:props.current.shotMode,directionX,directionZ,pullLength:pull};const points=engine.preview({mode:props.current.shotMode,originX:piece.body.position.x,originY:piece.body.position.y,originZ:piece.body.position.z,directionX,directionZ,pullLength:pull,steps:16});rt.dots.forEach((dot,index)=>{const point=points[index];dot.visible=Boolean(point);if(point)dot.position.set(...point);});}else if(props.current.cameraMode==='orbit'&&Math.hypot(dx,dy)>7){rt.orbiting=true;rt.yaw-=gesture.dx*.006;rt.pitch=Math.max(.38,Math.min(1.08,rt.pitch+gesture.dy*.004));updateCamera(rt);}touch.lastX=x;touch.lastY=y;},onPanResponderRelease:()=>{const rt=runtime.current,touch=rt?.touch;if(!rt||!touch)return;if(touch.aiming&&touch.command&&props.current.interactionUnlocked){engine.launch(touch.pieceId,touch.command);}else if(!rt.orbiting&&touch.pieceId!==null&&props.current.interactionUnlocked){engine.select(touch.pieceId);}else if(!rt.orbiting&&touch.pieceId===null){engine.clearSelection();}rt.dots.forEach(dot=>dot.visible=false);rt.touch=null;rt.orbiting=false;},onPanResponderTerminate:()=>{const rt=runtime.current;if(rt){rt.dots.forEach(dot=>dot.visible=false);rt.touch=null;rt.orbiting=false;}}}),[engine]);
 useEffect(()=>()=>{const rt=runtime.current;if(rt){rt.disposed=true;if(rt.frame)cancelAnimationFrame(rt.frame);try{rt.renderer.dispose();}catch{}runtime.current=null;}},[engine]);
 const onContextCreate=async gl=>{const canvas=canvasShim(gl),renderer=new THREE.WebGLRenderer({canvas,context:gl,antialias:true,alpha:false});renderer.setPixelRatio(1);renderer.setSize(gl.drawingBufferWidth,gl.drawingBufferHeight,false);renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;renderer.outputColorSpace=THREE.SRGBColorSpace;const scene=new THREE.Scene();scene.background=new THREE.Color(ASHYK_SCENE_MATERIALS.background);const camera=new THREE.PerspectiveCamera(42,gl.drawingBufferWidth/Math.max(1,gl.drawingBufferHeight),.5,110);const rt={gl,renderer,scene,camera,visuals:new Map(),raycaster:new THREE.Raycaster(),pointer:new THREE.Vector2(),boardPlane:new THREE.Plane(new THREE.Vector3(0,1,0),0),hitPoint:new THREE.Vector3(),yaw:.76,pitch:.63,dots:[],touch:null,orbiting:false,disposed:false,frame:0};runtime.current=rt;updateCamera(rt);scene.add(new THREE.HemisphereLight(0xdde9d7,0x20160f,1.7));const sun=new THREE.DirectionalLight(0xffefd0,2.8);sun.position.set(22,30,18);sun.castShadow=true;scene.add(sun);const board=new THREE.Mesh(new THREE.CylinderGeometry(HALF,HALF,.5,72),new THREE.MeshStandardMaterial(ASHYK_SCENE_MATERIALS.board));board.position.y=-.15;board.receiveShadow=true;scene.add(board);const field=new THREE.Mesh(new THREE.CircleGeometry(HALF-.95*BOARD_SCALE,72),new THREE.MeshStandardMaterial({...ASHYK_SCENE_MATERIALS.board,color:'#c8c9c5',roughness:.97,metalness:.02}));field.rotation.x=-Math.PI/2;field.position.y=.128;field.receiveShadow=true;scene.add(field);const ring=new THREE.Mesh(new THREE.RingGeometry(10.7*BOARD_SCALE,11.2*BOARD_SCALE,96),new THREE.MeshBasicMaterial({color:ASHYK_COLORS.ring,transparent:true,opacity:.56,side:THREE.DoubleSide,depthWrite:false}));ring.rotation.x=-Math.PI/2;ring.position.y=.158;scene.add(ring);const rim=new THREE.Mesh(new THREE.TorusGeometry(HALF-.5*BOARD_SCALE,1.05*BOARD_SCALE,12,96),new THREE.MeshStandardMaterial(ASHYK_SCENE_MATERIALS.rail));rim.rotation.x=Math.PI/2;rim.position.y=.55;rim.castShadow=true;rim.receiveShadow=true;scene.add(rim);const selection=new THREE.Mesh(new THREE.RingGeometry(.92,1.31,48),new THREE.MeshBasicMaterial({color:ASHYK_COLORS.accent,transparent:true,opacity:.78,side:THREE.DoubleSide,depthWrite:false}));selection.rotation.x=-Math.PI/2;selection.position.y=.178;selection.visible=false;scene.add(selection);rt.selection=selection;const dotGeo=new THREE.SphereGeometry(.14,10,6);rt.dots=Array.from({length:16},(_,index)=>{const dot=new THREE.Mesh(dotGeo,new THREE.MeshBasicMaterial({color:ASHYK_SCENE_MATERIALS.trajectory,transparent:true,opacity:Math.max(.52,.92-index*.035),depthTest:false,depthWrite:false}));dot.visible=false;scene.add(dot);return dot;});let last=global.performance?.now?.()??Date.now();const loop=time=>{if(rt.disposed)return;rt.frame=requestAnimationFrame(loop);const now=Number(time||Date.now()),dt=Math.min((now-last)/1000,1/20);last=now;engine.step(dt,now);updateCamera(rt);for(const piece of engine.getPieces()){let visual=rt.visuals.get(piece.id);if(!visual){visual=createAshykVisual(THREE,piece.id);rt.visuals.set(piece.id,visual);scene.add(visual);}visual.visible=piece.alive;if(!piece.alive)continue;visual.position.set(piece.body.position.x,piece.body.position.y,piece.body.position.z);visual.quaternion.set(piece.body.quaternion.x,piece.body.quaternion.y,piece.body.quaternion.z,piece.body.quaternion.w);}const selectedId=engine.getSelectedId(),piece=selectedId===null?null:engine.getPiece(selectedId);selection.visible=Boolean(piece);if(piece){selection.position.x=piece.body.position.x;selection.position.z=piece.body.position.z;}renderer.render(scene,camera);gl.endFrameEXP();};rt.frame=requestAnimationFrame(loop);};
 return <View style={styles.host} onLayout={event=>{layout.current=event.nativeEvent.layout||layout.current;}} {...pan.panHandlers}><GLView style={styles.gl} onContextCreate={onContextCreate}/></View>;}
const styles=StyleSheet.create({host:{flex:1,backgroundColor:ASHYK_SCENE_MATERIALS.background},gl:{flex:1}});
