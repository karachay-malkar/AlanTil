name: Bootstrap AlanTil 16.6.5 staging

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  stage:
    runs-on: ubuntu-latest
    steps:
      - run: echo "bootstrap paused while payload is replaced"
