name: Apply 16.6.6 Temporary QA Helper

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary helper retained only until final 16.6.6 cleanup"
